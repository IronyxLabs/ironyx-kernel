syntax = "proto3";

service GenericAPI {
	rpc SendAsync (Request) returns (Reply);
}

message Request {
	string body = 1;
}

message Reply {
	string status = 1;
	string data = 2;
	Error error = 3;
}

message Error {
	string code = 1;
	string message = 2;
}